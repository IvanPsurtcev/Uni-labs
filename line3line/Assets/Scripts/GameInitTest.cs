using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameInitTest : MonoBehaviour
{
    public MiniGame game;
    // Start is called before the first frame update
    void Start()
    {
        game.Open();
    }
}
